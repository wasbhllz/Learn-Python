// 24.04.15
#include <stdio.h>
/*
int i;
int main(){
    i--;
    if(i>sizeof(i)) // 因为sizeof(i)返回的是size_t类型的无符号整形值，i是有符号数，所以i需要算数转换为用补码表示的无符号数进行比较，也就是i会变得非常大，所以该表达式会返回真
        printf(">\n");
    else
        printf("<\n");
    return 0;
}
*/ 
