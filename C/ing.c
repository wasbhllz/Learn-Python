// 单行注释
/* 多行
注释块 */

# include <stdio.h>
int main(){ 
   int num = 1;
   printf("先苦后甜才是第 %d 位.\n",num); 
   int a, b;
    a = 10;
    b = 2 * a;
    printf("%d的2倍是%d!", a, b);
   // system("pause")运行窗口暂停
   return 0;
   getchar(); // 函数运行窗口暂停
}
//忘记在预处理文件前打# 